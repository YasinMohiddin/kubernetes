---
apiVersion: v1
kind: Service
metadata:
  name: voting-app-service
  labels:
    author: yasin
spec:
  type: LoadBalancer
  ports:
    - targetPort: 80
      port: 80 
      nodePort: 30001
  selector:
      name: voting-app
...
