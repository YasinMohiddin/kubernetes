---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-app-deployment
  labels:
    author: yasin
spec:
  replicas: 1
  selector:
    matchLabels:
      name: worker-app
  template:
    metadata:
      name: worker-app-deployment
      labels:
        name: worker-app
    spec:
      containers:
        - name: myworker
          image: dockersamples/examplevotingapp_worker
...
